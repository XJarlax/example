
    function main(a) {
        var odd = [];
        for (var i = 0; i < a.length; i++) {
            if (a[i] % 2 === 1) {
                odd.push(a[i]);
            } else {
            }

        }

        return [odd, not_odd];
    }

    var odd = [0, 1, 2, 3, 4, 5];
    var new_array = []

    function test_1(a) {


        for (var i = 0; i < a.length; i++) {

            new_array.push(a[i]);
        }



        return new_array;
    }

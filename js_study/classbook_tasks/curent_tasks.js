/*

function curent_task_1(login) {

    let message_1 = '';
    let message_2 = '';
    let message_3 = '';

    if (login == 'Vasya') {
        message_1 = 'Hi';
    } else if (login == 'Director') {
        message_1 = 'Hello';
    } else if (login == '') {
        message_1 = 'No login';
    } else {
        message_1 = 'okaaaay';
    }



    message_2 = (login == 'Vasya') ? 'Hi' :
        (login == 'Director') ? 'Hello' :
            (login == '') ? 'No login' :
                'okaaaay';





                login === 'Vasya' ?
                message_3 = 'Hi' :
                login === 'Director' ?
                    message_3 = 'Hello' :
                    login === '' ?
                        message_3 = 'No login' : message_3 = 'okaaaay';



    const result = message_1 === message_2  && message_2 === message_3;
    return result
}




/**
*@param {age} number
* 
* @return {string} 

function age_check(age) {
    let result = '';
    if (age < 14 || age > 90) {
        result = 'hehehe'
    } else {
        result = 'Welcome!'
    }
    return result
}



function main_1() {
    var i = 3;

    while (i) {
        alert(i--);
    }


    for (j = 3; j > 0; j--) {
        alert(j)
    }
}





var i = 0;
while (i < 3) {
  alert( "номер " + i + "!" );
  i++;
}
-ghtlkfuf.n cjplfntkb ext,ybrf

function main() {
    for (var i = 0; i < 3; i++) {
        alert( "номер " + i + "!" );
      }


      -исходный код

      let j =0;
    while (j < 3) {
        alert( "номер " + j++ + "!" )ж
    }  -мой вариант

    что лучше и почему?
}


main();

/*Напишите цикл, который предлагает prompt ввести число, большее 100. Если посетитель ввёл другое число – попросить ввести ещё раз, и так далее.

Цикл должен спрашивать число пока либо посетитель не введёт число, большее 100, либо не нажмёт кнопку Cancel (ESC).

Предполагается, что посетитель вводит только числа. Предусматривать обработку нечисловых строк в этой задаче необязательно.


var num;

do {
  num = prompt("Введите число больше 100?", 0);
} while (num <= 100 && num != null);




function main(){
let t;

outer: if (t <= 100) {
    t = prompt('Number more than 100?', 0);
    if (t === null) { break outer }
} 
}


*/

/**
*@param {from} number
* @param {to} number
* @return {array} 


function main(from, to){
    let result = [];
    for (i = from; i <= to; i++){
        if (i > 1 && i % )
    }
}
*/

